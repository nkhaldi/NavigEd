#!/usr/bin/env python3

from csv import reader as csvReader

'''
def read_from_db(subj):
    fName = '../database/' + (subjDict[subj].lower()).replace(' ', '') + '.csv'
    with open(fName, 'r') as fd:
        reader = csvReader(fd)
        key = 0
        for row in reader:
            key = printSource(row, key)
'''

def get_subject(get_message):
    return get_message
